# 小説家になろう 縦書きPDF → 縦書きEPUB変換スクリプト仕様

状態：PDF→EPUB変換コードを実装済み。サンプル全体で構造検証を実施。Kindle上の表示・操作は未検証。

既存OSSの調査は終了した。PDF入力を維持し、`.dev/` の手動変換EPUBに準じた構造でリフロー・目次・右開きを実現できるという暫定前提で進める。最終的な互換性は生成物のKindle上での動作により確認する。初期対象はWindows、Python 3.12以上とする。

## 1. 目的

「小説家になろう」のサイトからダウンロードした縦書きPDFを、Kindleで読むための縦書きEPUBへ変換する。現在Wordとcalibreなどで行っている処理を、スクリプトによって完結させる。

出力対象はEPUBとする。AZW3への変換は対象外。

## 2. 実行に関する条件

- Word、calibreなどのアプリケーションのインストール・起動・操作を変換の前提としない。
- 手動でDOCXやEPUBを編集する工程を必要としない。
- Python 3.12以上を使用し、初期検証対象OSはWindowsとする。Linux用セットアップスクリプトも提供するが、Linuxでの変換動作保証は追加検証後とする。
- PDF解析はpdfplumber、読み順・段落・空行・見出しの復元は独自処理、EPUB生成はPython標準のXML・ZIPライブラリを使用する。Dockerは必須にしない。
- コマンドラインでPDFと出力先を指定する。変換中の通信は不要とする。
- Windowsでは `scripts/setup.ps1` で `.venv`、Linuxでは `scripts/setup.sh` で `.venv-linux` を作成し、固定バージョンの依存を導入する。再実行時は既存環境を再利用する。Python本体は事前導入とし、依存取得時のみ通信する。
- 入力PDFの取得およびKindleへの転送は変換処理の対象外とする。

## 3. 入力と参照資料

入力は、サイトからダウンロードした縦書きPDFファイルとする。まず以下のサンプルを使って実装・検証する。

| 資料（すべて `.dev/` 内） | 用途 |
| --- | --- |
| `小説家になろう　縦書きPDFをkindleで読める状態にする.txt` | 現在の手動処理を理解するための資料 |
| `N4251CR.pdf` | 実際の変換入力、本文や空行を確認するための原資料 |
| `無職転生-蛇足編-.docx` | Wordによる中間処理、特に見出し・目次構造の参考 |
| `無職転生 -蛇足編- - 株式会社ヒナプロジェクト.epub` | 縦書き・ページ進行方向・目次などの出力例 |

完成済みEPUBはすべての項目の正解データとはしない。特に空行は現在必要以上に詰められており、改善対象である。資料間に差がある場合は差分を記録し、本文や意味上の区切りを失わないよう判断する。

DOCXは調査・比較用であり、本番の変換に必須の入力とはしない。

ユーザー確認によれば、`.dev/` の完成EPUBはKindleでリフローと目次メニューに対応している。このファイルを動作実績のある参照とし、パッケージ構造、書誌・レイアウト情報、CSS、ナビゲーション定義を調査して、生成EPUBに必要な条件を具体化する。個々の設定が必要かどうかは未確定であり、構造比較とKindleでの確認により判断する。

## 4. 必須機能

### 4.1 本文の抽出と読み順

- PDFの縦書きの読み順を復元する。
- 本文の欠落、重複、列や段落の順序の入れ替わりを防ぐ。
- PDFの物理的な改ページを、そのまま文章の区切りとして扱わない。
- 句読点・括弧・記号・数字を意図せず置換・削除しない。
- ルビの有無と構造を調査する。存在する場合の親文字との対応やEPUBでの再現方法は、実装前に方針を決める。

### 4.2 目次

- PDFから章・各話の見出しを識別し、EPUBのナビゲーション用目次を生成する。
- 目次の各項目から、対応する章・各話の先頭へ移動できるようにする。
- Kindleの目次メニューに章・各話が表示され、そこから移動できることを必須とする。本文内の目次ページやEPUB内のリンクが存在するだけでは合格としない。
- 見出しの名称・順序を維持する。階層がある場合は参照資料と照合して扱いを決める。
- 中間DOCXの「見出し1」と完成済みEPUBの目次を比較し、抽出結果を検証する。
- 単なる太字、本文の一部、ページ番号などを見出しとして誤認しない。
- 見出しの検出規則はサンプルの調査によって決める。作品名や特定の見出し文字列だけに依存した実装を避ける。

### 4.3 縦書き

- 本文および章・各話の見出しを、上から下、列は右から左の縦書きで表示する。
- EPUBのスタイルに縦書き指定（`writing-mode: vertical-rl` 等）を設定する。
- 互換用のスタイルやメタデータは、採用するEPUB仕様と表示確認に基づいて決める。既存EPUBの設定を無条件にすべて複製しない。
- 単に設定が存在することだけでなく、対象のKindle環境で縦書きが維持されることを確認する。

### 4.4 ページめくりの方向

- 右開きのページ進行方向を設定する。
- EPUBのspineに `page-progression-direction="rtl"` を指定する。
- 縦書きのCSS指定とページ進行方向を別々に検証する。
- 具体的なタップ・スワイプ操作は閲覧環境に依存するため、対象環境で前後のページへ意図した方向に移動できることを確認する。

### 4.5 空行・段落

- 変換処理やPDFのページ境界に由来する不要な空行を減らす。
- 段落や場面転換など、文章の区切りに必要な空行を維持する。
- 空行を一括削除したり、すべての空白の連続を機械的に詰めたりしない。
- PDF上の配置、段落間の距離、前後の本文、DOCXの段落構造を調べ、改行・段落・空行を区別する規則を設計する。
- ページ末尾と次ページ先頭の接続では、継続する本文と意味のある区切りを区別する。
- 必要な空行の判定が曖昧な場合は、黙って削除せず、確認対象として記録する。
- 空行の検証基準は元PDFなどから代表箇所を選んで明文化する。既存EPUBとの完全一致を合格条件にしない。
- 必要な区切りが空の段落ではなく、段落後の余白として保存されている場合も考慮する。縦書きの段落進行方向に適切な間隔として再現し、横書き用の物理方向の余白指定をそのまま引き継がない。

### 4.6 ページ番号の除去

- PDFの各ページに印刷された、本文として意味を持たないページ番号を出力本文から除去する。
- 本文中の数字、話数、章番号、日付などは保持する。
- 数字だけという理由では削除しない。位置、繰り返し、書式などを調査して判定する。
- 既存EPUB固有のクラス名に依存する削除処理を採用しない。

### 4.7 EPUBの生成

- 本文、スタイル、書誌情報、目次、読み順を含むEPUBを生成する。
- Kindleに転送した後もリフロー対応として扱われ、文字サイズを変更できることを必須とする。元の固定的なレイアウトを維持する代替処理となり、文字サイズを変更できない場合は不合格とする。
- リンク先の欠落、目次と本文の不一致、パッケージ内の参照切れがないことを検証する。
- 書籍の言語を日本語に設定する。
- タイトル・著者などの取得元と指定方法は調査後に決定する。取得できない情報を推測で補わない。
- EPUBのバージョン、ファイル分割単位、埋め込みフォントの要否は未決定とする。

## 5. ファイル保護と出力

- `.dev/` 内は参照専用とし、編集・上書き・削除・追加作成を行わない。
- 入力ファイルを変更しない。
- 変換結果、抽出データ、ログ、一時ファイル、検証用画像はすべて `.dev/` の外に作成する。
- 入出力先が同じ場合や、出力先が `.dev/` 内の場合は書き込みを拒否する。
- 既存の出力ファイルを意図せず上書きしない。上書きの操作方法は実行インターフェースの設計時に定める。

## 6. 処理構成案

技術選定前の論理構成として、次の工程を分ける。

1. PDFの文字・位置・書式などの取得。
2. 縦書きの読み順、本文、段落、空行、見出しの復元。
3. ページ番号などの不要要素の識別と除去。
4. 章・各話の構造と目次の構築。
5. 縦書き・右開きのEPUBの生成。
6. 構造検証および比較用レポートの出力。

中間情報を保持し、本文抽出の問題とEPUB生成の問題を切り分けられる設計とする。中間形式の具体的な仕様は未決定とする。

## 7. エラーと診断

- ファイルがない、PDFを読めない、文字を抽出できない、EPUB生成に失敗した場合は、原因を確認できるエラーを出す。
- 文字を抽出できないPDFを、空のEPUBとして成功扱いにしない。
- 見出しを検出できない場合や空行・数字の判定が曖昧な場合は、診断情報を残す。
- 除去したページ番号や、検出した目次の一覧を確認できるようにする。
- OCRによる画像PDFへの対応は現時点では必須とせず、入力サンプルの調査後に必要性を判断する。

## 8. 検証と受け入れ基準

`.dev/` の実ファイルを使用して変換と動作確認を実施する。一部の構造比較は実施済みだが、資料の全面的な解析やPDFからの変換は未実施である。以下は開発する変換処理の受け入れ基準とする。

| 対象 | 合格条件 |
| --- | --- |
| 実行 | 選定したランタイムと依存ライブラリで、Word・calibreを呼び出さずにPDFからEPUBを生成できる |
| 本文 | 全体の抽出差分を確認し、代表箇所で読み順・欠落・重複に問題がない |
| 目次 | Kindleの目次メニューに表示され、全項目の名称・順序・リンク先が正しく、各話の先頭に移動できる |
| リフロー | Kindleへの転送後も文字サイズを変更でき、本文が読み直し可能な配置に再組版される |
| 縦書き | 本文と見出しが対象Kindle環境で縦書き表示される |
| ページ進行 | 対象Kindle環境で右開きのページ送りになる |
| 不要な空行 | 抽出由来の余分な空行が、選定した検証箇所で除去される |
| 必要な空行 | 場面転換など、事前に合意した検証箇所の空行が維持される |
| ページ番号 | 印刷ページ番号が本文に残らず、本文中の数字や話数は維持される |
| EPUB構造 | 目次リンク、本文参照、パッケージ構造にエラーがない |
| 元ファイル保護 | 検証前後のハッシュなどで `.dev/` 内の既存ファイルが変化していないことを確認できる |

自動検証と表示確認を併用する。対象Kindleの機種・アプリ・転送方法は未決定とし、実機または対象アプリで確認するまではKindle上の表示検証完了とはしない。

コードのテストには、見出しの誤認、ページをまたぐ段落、必要な空行の消失、本文数字の誤削除などの回帰を検出できる小さな独自データを用いる。小説本文を公開テストデータに転記しない。

## 9. GitHubとライセンス

- コードはGitHubへの登録を予定する。
- コード、依存関係の定義、利用手順、テスト、ライセンスをリポジトリで管理する。
- `.dev/`、変換結果、一時ファイル、本文を含むログや検証データは `.gitignore` で除外する。
- 登録前に追跡対象を確認する。`.gitignore` だけでは追跡済みファイルが除外されない点に注意する。
- 作成コードと付属文書のライセンスはMITとする。参照資料、小説本文、生成EPUBには適用しない。
- 依存ライブラリおよび同梱物には配布元のライセンスを適用し、再配布時に必要な表示を確認する。

## 10. 実装前に決める事項

- EPUBの仕様検証に使用する検証ツール。
- 対象とするPDF形式の範囲と、レイアウト差への対応範囲。
- 見出しの検出規則および目次階層。
- 段落と空行の復元規則、および保持すべき空行の具体例。
- ルビ、挿絵、特殊文字などの対応範囲。
- EPUBのバージョン、書誌情報の指定方法。
- 実行方法、設定項目、出力先、上書き指定。
- 検証対象のKindle環境と転送方法。

次の工程では参照資料を解析し、未決定事項のうち抽出方式・目次・空行復元に関わる部分を具体化する。その結果に基づいて実装を進める。

実装後の決定：EPUB 3を生成し、EPUB 3のナビゲーション文書と互換用NCXを含める。話・後書き・注意事項・表紙を個別XHTMLとし、目次は参照EPUBに合わせた1階層とする。書名と著者はPDF表紙から取得し、CLIで上書きできる。`--overwrite` を指定した場合のみ既存の出力と診断レポートを置き換える。文字抽出できない画像PDFはエラーとする。

## 11. PDF抽出の限定検証

`tools/probe_pdf.py` により、サンプルPDFの物理4～5ページにある第1話冒頭20段落を検証した。使用ライブラリはpdfplumber 0.11.9。その後の基本技術の決定でpdfplumberを採用した。

- 文字情報からOCRなしで見出しと本文を取得できた。
- 列を右から左、列内を上から下に並べ、字下げを手掛かりに段落を復元した。
- 通常の列間隔約22.68ptに対し、約45.35ptの間隔を空行1つとして検出した。
- 本文1・4・6・11・13・16段落目の後に空行を復元できた。
- 冒頭20段落の本文は参照EPUBと文字列が一致した。ページをまたぐ15段落目も接続できた。
- 太字の第1話見出しを本文と分離した。
- この範囲で本文とサイズが異なる文字は印刷ページ番号3・4であり、本文から除外できた。
- PDFの2ページを画像化して読み順と空白列を目視確認した。入力PDFと参照EPUBは処理前後のSHA-256が一致した。

この検証は対象範囲に限定した試作であり、作品全体への対応を保証しない。小さい文字を除外する現行処理はルビなどにも影響するため、本実装のページ番号判定には位置などを加える必要がある。字下げのない段落、括弧から始まる折り返し、ページ境界の空行、複数列にわたる見出し、挿絵は未検証。文章の途中で終わる最終列の扱いも本実装で検討する。

本文を含む抽出結果・検証レポート・画像は `output/pdf-probe/` に保存し、コミットしない。

## 12. 実装と残る検証

`narou_epub.py` で全文を変換し、表紙を含む37項目の目次を生成した。印刷ページ番号を位置とサイズで識別し、23箇所の小字グループをルビとして親文字に結び付けた。末尾の横書き奥付は本文末へ収録した。参照EPUBとの各節の文字列比較では、本文の大半が一致する。差異には参照EPUB側の記号置換、ルビ、段落分割が含まれる。参照EPUBは空行の正解データではないため、段落数の一致は合格基準にしない。

診断JSONにページ番号、見出し、ルビ判定とページ境界の確認対象を出力する。ページをまたぐ空行の有無と、配置が曖昧なルビについては目視確認が必要である。Kindleの目次メニュー、縦書き表示、リフロー、ページ送りは実機・対象アプリで確認するまで未達の受け入れ項目とする。
